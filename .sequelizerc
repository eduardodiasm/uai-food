require('dotenv').config()
const path = require('path')

module.exports = {
  config: path.resolve(__dirname, 'lib', 'infrastructure', 
  'database', 'config', 'postgres.js'),

  'migrations-path': path.resolve(__dirname, 'lib', 
  
  'infrastructure','database', 'sequelize', 'migrations'),
}